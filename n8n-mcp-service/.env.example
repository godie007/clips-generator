# n8n MCP service — variables de entorno
# Copia a .env y ajusta (especialmente N8N_MCP_API_KEY).

# URL base de n8n (donde corre tu instancia)
N8N_MCP_BASE_URL=http://localhost:5678

# API Key / JWT de n8n (Settings > n8n API en la interfaz)
# Ejemplo (sustituir por tu key):
# N8N_MCP_API_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
N8N_MCP_API_KEY=

# Servidor MCP (puerto por defecto 8010 para no chocar con image_gen en 8001)
N8N_MCP_SERVER_HOST=127.0.0.1
N8N_MCP_SERVER_PORT=8010
N8N_MCP_LOG_LEVEL=INFO
