# mcp-audio-gen — Chatterbox TTS
# Copiar a .env y ajustar

AUDIO_MCP_OUTPUT_DIR=./outputs/audio
AUDIO_MCP_SERVER_HOST=127.0.0.1
AUDIO_MCP_SERVER_PORT=8003
AUDIO_MCP_REST_PORT=8004
AUDIO_MCP_LOG_LEVEL=INFO
# cuda, cpu o mps
AUDIO_MCP_DEVICE=cuda
# Voz de referencia por defecto (~10 s). WAV o MP3. Se usa si el request no envía audio_prompt_path.
# Ruta relativa al directorio genAudio. Por defecto: voiceReference.mp3
# AUDIO_MCP_DEFAULT_AUDIO_PROMPT_PATH=voiceReference.mp3
