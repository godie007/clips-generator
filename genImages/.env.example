# ════════════════════════════════════════════════════════════
#  mcp-image-gen — Variables de entorno
#  Copiar como .env y ajustar para tu setup
# ════════════════════════════════════════════════════════════

# ── ComfyUI backend ──────────────────────────────────────────
# Opcional: ruta a ComfyUI si no está en genImages/ComfyUI (usado por scripts run_comfyui_gpu.ps1)
# COMFYUI_DIR=C:\ruta\a\ComfyUI
IMAGE_MCP_COMFYUI_URL=http://127.0.0.1:8188
# Tiempo máximo de espera por imagen (segundos). 4K puede tardar 3–10 min; usa 600–900
IMAGE_MCP_COMFYUI_TIMEOUT=600

# ── Modelo FLUX.1 ────────────────────────────────────────────
# Opciones recomendadas para RTX 4060 8GB:
#   flux1-schnell-fp8.safetensors   → más rápido, calidad top, FP8 = cabe en 8GB
#   flux1-dev-fp8.safetensors       → más calidad, más lento, 20-30 steps
#   flux1-schnell-bnb-nf4.safetensors → cuantizado 4-bit, máxima velocidad
IMAGE_MCP_DEFAULT_MODEL=flux1-schnell-fp8.safetensors

# ── Dimensiones por defecto (4K, calidad profesional) ────────
# 4K UHD: 3840x2160. Si te queda corto de VRAM usa 2048x1152.
IMAGE_MCP_DEFAULT_WIDTH=3840
IMAGE_MCP_DEFAULT_HEIGHT=2160
IMAGE_MCP_DEFAULT_STEPS=8
IMAGE_MCP_DEFAULT_GUIDANCE=4.0

# ── Output ───────────────────────────────────────────────────
IMAGE_MCP_OUTPUT_DIR=./outputs/images

# ── Servidor MCP ─────────────────────────────────────────────
# IMPORTANTE: usar 127.0.0.1 para seguridad (no exponer a red local)
IMAGE_MCP_SERVER_HOST=127.0.0.1
IMAGE_MCP_SERVER_PORT=8001
IMAGE_MCP_LOG_LEVEL=INFO
